import { Observable } from "rxjs";
import { WebSocketSubjectConfig } from "rxjs/webSocket";
import { StreamRequestOptions } from "../internal/types";
declare type Command = {
    name: string;
    command: <T>(config: string | WebSocketSubjectConfig<T>) => Observable<T>;
};
declare type RequestCommand = {
    name: string;
    command: <T>(config: string | WebSocketSubjectConfig<T>, options: Partial<StreamRequestOptions<T>>) => Promise<T[] | void>;
};
export declare const streamCommand: Command;
export declare const streamRequestCommand: RequestCommand;
export declare const addStreamCommands: () => void;
export {};
